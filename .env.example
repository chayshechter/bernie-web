# Sentry error monitoring.
# DSN from your Sentry project: Settings -> Projects -> [project] -> Client Keys (DSN).
# Safe to expose publicly (a DSN only permits sending events, not reading them).
# Leave unset to disable error reporting (e.g. local dev).
VITE_SENTRY_DSN=
